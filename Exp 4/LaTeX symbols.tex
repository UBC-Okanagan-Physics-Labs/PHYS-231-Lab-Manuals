\documentclass{article}
\usepackage[latin1]{inputenc} % Fixa едц
\usepackage{color}
\usepackage{psfrag}
\usepackage{setspace}
\usepackage{amsmath}
\usepackage{mathrsfs}
\usepackage{units}
\usepackage{upgreek}

\usepackage{amsfonts} % needed for bold Greek, Fraktur, and blackboard bold
\usepackage{graphicx}

\usepackage{amssymb}
\usepackage{verbatim}
\definecolor{pink}{rgb}{1,0.5,0.5} % color values Red, Green, Blue

\begin{document}
%\pagecolor{black} % Background color
%\color{white}     % Text color


\begin{gather}
R_0=\nonumber\\
R_\delta+R_\mathrm{ex}+R_{\varepsilon^{\prime\!\prime}}\nonumber
\end{gather}

$$
R
$$

$$
V_R
$$

$$
V_0
$$

\begin{center}
function\\
generator
\end{center}

$$
\varepsilon^\prime C
$$


$$
\varepsilon^\prime C_\mathrm{eff}(\omega,\sigma)
$$

$$
R_\mathrm{eff}(\omega,\sigma)
$$

$$
R_\delta+R_\mathrm{ex}+R_{\varepsilon^{\prime\!\prime}}
$$


$$
R_\sigma
$$

$$
R_\mathrm{tot}
$$


$$
R_\delta
$$

$$
I
$$


$$
\delta
$$

$$
2\pi r_0
$$

$$
L
$$

$$
R_\mathrm{Al}
$$

$$
C
$$

$$
r_0
$$

$$
w
$$

$$
t
$$

$$
\ell
$$

$$
Z_\mathrm{p}=R_\mathrm{p}+\frac{1}{j\omega C_\mathrm{p}}
$$

$$
C_\mathrm{f}
$$

$$
\left(\varepsilon_\mathrm{r}-j\frac{\sigma_\mathrm{dc}}{\omega\varepsilon_0}\right)C_0
$$

I think I'm in \textcolor{red}{love} with.

$$
\boldsymbol{E}(\boldsymbol{z},t)=E_0 e^{i(\omega t-kz)}\hat x
$$
$$
\boldsymbol{H}(\boldsymbol{z},t)=H_0 e^{i(\omega t-kz)}\hat y
$$

$$
Z_0\equiv\frac{\vert\boldsymbol{E}\vert}{\vert\boldsymbol{H}\vert}
$$

$$
\nabla\times\boldsymbol{E}=-\mu_0\frac{\partial\boldsymbol{H}}{\partial t}
$$

$$
Z_0=\sqrt{\frac{\mu_0}{\varepsilon_0}}\approx377~\Omega
$$

$$
Z_\mathrm{S}=R_\mathrm{S}+iX_\mathrm{S}\equiv\left.\frac{E_x(z,t)}{H_y(z,t)}\right\vert_{z=0}
$$

$$
\boldsymbol{E}(\boldsymbol{z},t)=E_0 e^{i\omega t-\kappa z}\hat x
$$
$$
\boldsymbol{H}(\boldsymbol{z},t)=H_0 e^{i\omega t-\kappa z}\hat y
$$

$$
Z_\mathrm{S}=\frac{i\mu_0\omega}{\kappa}=\sqrt{\frac{\mu_0}{\varepsilon_0\varepsilon}}=\sqrt{\frac{i\mu_0\omega}{\sigma}}
$$

$$
\sigma(\omega)=\sigma_0
$$

$$
Z_\mathrm{S}=R_\mathrm{S}+iX_\mathrm{S}=\sqrt{\frac{i\mu_0\omega}{\sigma(\omega)}}
$$

$$
\sqrt{i}=\frac{i+1}{\sqrt{2}}
$$

\textcolor{yellow}{
$$
\delta=\sqrt{\frac{2}{\mu_0\omega\sigma_0}}
$$
$$
R_\mathrm{S}=\sqrt{\frac{\mu_0\omega}{2\sigma_0}}=\frac{1}{\sigma_0\delta}
$$
}

\end{document} 